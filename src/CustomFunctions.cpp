#include "HH.h"

namespace my
{
        sf::Vector2f vecX( float x )
        {
                return sf::Vector2f( x, 0 );
        }

        sf::Vector2f vecY( float y )
        {
                return sf::Vector2f( 0, y );
        }
}
